# Copy this file to .env locally and fill in your real Neon connection string.
# On Render, you'll set DATABASE_URL directly in the dashboard instead of a file.
DATABASE_URL=postgresql://user:password@your-neon-host/dbname?sslmode=require
